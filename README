To launch the docker image via the button launch the site chalenge you must have docker installed on the machine and download the file accessible on the site and
with the command prompt launch : docker load -i "chemin du fichier/picky-website.tar" et ensuite docker run -p 3000:3000 picky-website
IDE: vscode
services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend:/app
    command: python app.py
to launch the site: execute the file app.py
